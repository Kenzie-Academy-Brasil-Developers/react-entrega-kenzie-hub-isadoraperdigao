import styled from "styled-components";

export const StyledTechList = styled.ul`
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 1em;
    padding: 0.75em 0.531em;
    background-color: var(--color-gray-3);
    list-style: none;
    border-radius: 4px;
`